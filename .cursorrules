# Git Commit Message Rules

## 🚨 CRITICAL: Commit Message Language

**ALL commit messages MUST be written in KOREAN (한글), not English.**

When generating commit messages:
- Write the `<subject>` in KOREAN
- Write the `<body>` in KOREAN  
- Keep `<type>` and `<scope>` in English (e.g., feat, fix, docs)
- The description after the colon must be in KOREAN

## Format

```
<type>(<scope>): <한글로 작성된 제목>

<한글로 작성된 본문>

<footer>
```

## Examples

✅ CORRECT:
```
feat(products): 상품 검색 기능 추가
fix(auth): 로그인 버튼 클릭 오류 수정
docs(readme): 설치 가이드 업데이트
refactor(api): 상품 API 에러 처리 개선
chore(deps): 패키지 의존성 업데이트
```

❌ INCORRECT:
```
feat(products): add product search functionality
fix(auth): resolve login button click issue
docs(readme): update installation guide
```

## Commit Types

- **feat**: 새로운 기능 추가
- **fix**: 버그 수정
- **docs**: 문서 작업
- **style**: 코드 스타일 수정
- **refactor**: 코드 리팩토링
- **test**: 테스트 코드 추가/수정
- **chore**: 빌드 설정, 패키지 업데이트 등

## Detailed Examples with Body

```
feat(market-prices): 시장 가격 조회 API 추가

사용자가 실시간 시장 가격을 조회할 수 있도록
KAMIS API와 AT API를 통합한 기능을 구현했습니다.

- KAMIS API 연동
- AT API 연동
- 에러 처리 및 로깅 추가

Closes #123
```

```
fix(orders): 주문 상세 페이지 로딩 오류 수정

주문 ID가 없을 때 발생하는 에러를 처리하고
사용자에게 적절한 메시지를 표시하도록 개선했습니다.
```

---

**REMEMBER: Always use KOREAN for commit message descriptions!**
**기억하세요: 커밋 메시지 설명은 항상 한글로 작성해야 합니다!**


---

**REMEMBER: Always use KOREAN for commit message descriptions!**
**기억하세요: 커밋 메시지 설명은 항상 한글로 작성해야 합니다!**

---

---

# 반응형 디자인 & 다크모드 필수 규칙 (Responsive Design & Dark Mode Requirements)

## 🚨 CRITICAL: 반응형 디자인 필수 구현

**모든 페이지와 컴포넌트는 반드시 다음 4가지 화면 크기에 대해 반응형으로 구현되어야 합니다:**

1. **모바일 (Mobile)**: 320px ~ 639px
2. **태블릿 (Tablet)**: 640px ~ 1023px  
3. **노트북 (Laptop)**: 1024px ~ 1279px
4. **데스크탑 (Desktop)**: 1280px 이상

### 반응형 구현 규칙

**모든 컴포넌트와 페이지는 Mobile First 방식으로 작성해야 하며, 각 브레이크포인트에서 최적화된 레이아웃을 제공해야 합니다.**

✅ CORRECT:
// 그리드 레이아웃 예시
<div className="
  grid grid-cols-1          // 모바일: 1열
  sm:grid-cols-2            // 태블릿 (640px+): 2열
  lg:grid-cols-3            // 노트북 (1024px+): 3열
  xl:grid-cols-4            // 데스크탑 (1280px+): 4열
  gap-4 sm:gap-6 lg:gap-8   // 반응형 간격
">
  {/* 컨텐츠 */}
</div>

// 타이포그래피 예시
<h1 className="
  text-xl              // 모바일: 20px
  sm:text-2xl          // 태블릿: 24px
  lg:text-3xl          // 노트북: 30px
  xl:text-4xl          // 데스크탑: 36px
  font-bold
">
  제목
</h1>

// 패딩/마진 예시
<div className="
  p-4                  // 모바일: 16px
  sm:p-6               // 태블릿: 24px
  lg:p-8               // 노트북: 32px
  xl:p-12              // 데스크탑: 48px
">
  컨텐츠
</div>

// 네비게이션 예시
<nav className="
  flex-col              // 모바일: 세로 배치
  sm:flex-row           // 태블릿: 가로 배치
  gap-2 sm:gap-4
">
  {/* 메뉴 아이템 */}
</nav>### 브레이크포인트 정의

**Tailwind CSS 브레이크포인트를 사용하며, 다음 기준을 따릅니다:**

- `sm:` (640px+): 태블릿 세로 모드
- `md:` (768px+): 태블릿 가로 모드
- `lg:` (1024px+): 노트북
- `xl:` (1280px+): 데스크탑
- `2xl:` (1536px+): 대형 데스크탑

### 반응형 체크리스트

**새로운 페이지나 컴포넌트를 만들 때 다음을 확인하세요:**

- [ ] 모바일 (320px)에서 레이아웃이 깨지지 않는가?
- [ ] 태블릿 (768px)에서 최적화된 레이아웃을 제공하는가?
- [ ] 노트북 (1024px)에서 적절한 컬럼 수를 사용하는가?
- [ ] 데스크탑 (1280px+)에서 여백과 간격이 적절한가?
- [ ] 모든 화면 크기에서 텍스트가 읽기 쉬운가?
- [ ] 터치 영역이 모바일에서 최소 44x44px인가?
- [ ] 이미지와 미디어가 반응형인가?
- [ ] 테이블이 작은 화면에서 스크롤 가능한가?

---

## 🌓 CRITICAL: 다크모드 필수 구현

**모든 페이지와 컴포넌트는 반드시 라이트모드와 다크모드를 모두 지원해야 합니다.**

### 다크모드 구현 규칙

**Tailwind CSS의 `dark:` 접두사를 사용하여 다크모드를 구현합니다.**

✅ CORRECT:
// 배경색 예시
<div className="
  bg-white dark:bg-gray-900
  text-gray-900 dark:text-gray-100
">
  컨텐츠
</div>

// 카드 예시
<div className="
  bg-white dark:bg-gray-800
  text-gray-900 dark:text-gray-100
  border border-gray-100 dark:border-gray-700
  shadow-sm dark:shadow-gray-900/30
  rounded-lg
  p-6
">
  카드 내용
</div>

// 버튼 예시
<button className="
  bg-emerald-500 hover:bg-emerald-600
  dark:bg-emerald-600 dark:hover:bg-emerald-500
  text-white
  px-4 py-2.5 rounded-lg
  shadow-sm hover:shadow-md
  transition-all duration-300
">
  확인
</button>

// 입력 필드 예시
<input
  type="text"
  className="
    w-full px-4 py-2.5
    bg-white dark:bg-gray-800
    text-gray-900 dark:text-gray-100
    border border-gray-300 dark:border-gray-600
    rounded-lg
    focus:ring-2 focus:ring-emerald-500
    dark:focus:ring-emerald-400
    placeholder:text-gray-400 dark:placeholder:text-gray-500
  "
/>

// 텍스트 계층 예시
<h1 className="text-gray-900 dark:text-gray-100">제목</h1>
<p className="text-gray-600 dark:text-gray-400">본문</p>
<span className="text-gray-400 dark:text-gray-500">보조 텍스트</span>### 다크모드 색상 매핑

**라이트모드와 다크모드 색상 매핑 규칙:**

| 요소 | 라이트모드 | 다크모드 |
|------|-----------|---------|
| 페이지 배경 | `bg-gray-50` | `dark:bg-gray-900` |
| 카드 배경 | `bg-white` | `dark:bg-gray-800` |
| 주요 텍스트 | `text-gray-900` | `dark:text-gray-100` |
| 본문 텍스트 | `text-gray-600` | `dark:text-gray-400` |
| 보조 텍스트 | `text-gray-400` | `dark:text-gray-500` |
| 테두리 | `border-gray-100` (카드), `border-gray-200` (일반) | `dark:border-gray-700` |
| 입력 필드 배경 | `bg-white` | `dark:bg-gray-800` |
| 입력 필드 테두리 | `border-gray-300` | `dark:border-gray-600` |
| 그림자 | `shadow-sm` | `dark:shadow-gray-900/30` |

### Primary 색상 다크모드 적용

**Emerald Green Primary 색상은 다크모드에서도 동일하게 사용하되, 약간 더 밝은 톤을 사용합니다:**

✅ CORRECT:
// Primary 버튼
<button className="
  bg-emerald-500 hover:bg-emerald-600
  dark:bg-emerald-600 dark:hover:bg-emerald-500
  text-white
">
  확인
</button>

// Primary 링크
<a className="
  text-emerald-500 hover:text-emerald-600
  dark:text-emerald-400 dark:hover:text-emerald-300
">
  링크
</a>

// Primary 강조
<span className="
  text-emerald-500
  dark:text-emerald-400
">
  강조 텍스트
</span>### 다크모드 토글 구현

**다크모드 토글은 `next-themes` 라이브러리를 사용하여 구현합니다.**

✅ CORRECT:x
// app/layout.tsx 또는 _app.tsx
import { ThemeProvider } from 'next-themes';

export default function RootLayout({ children }) {
  return (
    <html lang="ko" suppressHydrationWarning>
      <body>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}

// 다크모드 토글 버튼 컴포넌트
'use client';
import { useTheme } from 'next-themes';
import { Moon, Sun } from 'lucide-react';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();

  return (
    <button
      onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')}
      className="
        p-2 rounded-xl
        bg-gray-100 dark:bg-gray-800
        text-gray-900 dark:text-gray-100
        hover:bg-gray-200 dark:hover:bg-gray-700
        transition-colors duration-300
      "
    >
      {theme === 'dark' ? <Sun size={20} /> : <Moon size={20} />}
    </button>
  );
}### 다크모드 체크리스트

**새로운 페이지나 컴포넌트를 만들 때 다음을 확인하세요:**

- [ ] 모든 배경색에 다크모드 변형이 있는가? (`dark:bg-*`)
- [ ] 모든 텍스트 색상에 다크모드 변형이 있는가? (`dark:text-*`)
- [ ] 모든 테두리에 다크모드 변형이 있는가? (`dark:border-*`)
- [ ] 모든 그림자에 다크모드 변형이 있는가? (`dark:shadow-*`)
- [ ] 입력 필드가 다크모드에서도 잘 보이는가?
- [ ] 버튼이 다크모드에서도 적절한 대비를 가지는가?
- [ ] 이미지와 아이콘이 다크모드에서도 잘 보이는가?
- [ ] 다크모드에서 색상 대비가 WCAG 기준을 만족하는가?

---

## 🔄 반응형 + 다크모드 통합 예시

**실제 컴포넌트에서는 반응형과 다크모드를 함께 구현합니다:**

✅ CORRECT:
export function ProductCard({ product }) {
  return (
    <div className="
      // 반응형 그리드
      grid grid-cols-1
      sm:grid-cols-2
      lg:grid-cols-3
      xl:grid-cols-4
      gap-4 sm:gap-6
      
      // 다크모드 배경
      bg-white dark:bg-gray-800
      
      // 다크모드 텍스트
      text-gray-900 dark:text-gray-100
      
      // 다크모드 테두리 
      border border-gray-100 dark:border-gray-700
      
      // 다크모드 그림자 
      shadow-sm dark:shadow-gray-900/30
      
      // 반응형 패딩
      p-4 sm:p-6 lg:p-8
      
      // 모서리 
      rounded-lg
      
      // 호버 효과 (다크모드 포함)
      hover:shadow-md dark:hover:shadow-gray-900/50
      hover:-translate-y-1
      transition-all duration-300
    ">
      <h3 className="
        text-lg sm:text-xl lg:text-2xl
        font-bold
        text-gray-900 dark:text-gray-100
      ">
        {product.name}
      </h3>
      <p className="
        text-sm sm:text-base
        text-gray-600 dark:text-gray-400
      ">
        {product.description}
      </p>
      <button className="
        w-full
        mt-4
        bg-emerald-500 hover:bg-emerald-600
        dark:bg-emerald-600 dark:hover:bg-emerald-500
        text-white
        px-4 py-2.5
        sm:px-6 sm:py-3
        rounded-lg
        shadow-sm hover:shadow-md
        transition-all duration-300
      ">
        구매하기
      </button>
    </div>
  );
}---

## 🚫 금지 사항

**다음은 절대 사용하지 마세요:**

❌ 금지:
- 다크모드 없이 라이트모드만 구현
- 고정된 너비 사용 (`w-[800px]` 등, 반응형이 아닌 경우)
- 모바일에서만 작동하는 레이아웃 (태블릿/데스크탑 미지원)
- `dark:` 접두사 없이 색상만 사용
- 하드코딩된 색상 값 (`bg-[#ffffff]` 등, 다크모드 변형 없음)
- 미디어 쿼리 직접 사용 (Tailwind 브레이크포인트 사용 권장)

---

## 📋 최종 체크리스트

**새로운 페이지나 기능을 구현할 때 반드시 확인하세요:**

### 반응형 체크리스트
- [ ] 모바일 (320px+) 레이아웃 구현
- [ ] 태블릿 (640px+) 레이아웃 구현
- [ ] 노트북 (1024px+) 레이아웃 구현
- [ ] 데스크탑 (1280px+) 레이아웃 구현
- [ ] 모든 화면 크기에서 테스트 완료

### 다크모드 체크리스트
- [ ] 라이트모드 스타일 구현
- [ ] 다크모드 스타일 구현 (`dark:` 접두사 사용)
- [ ] 다크모드 토글 기능 구현
- [ ] 다크모드에서 색상 대비 확인
- [ ] 다크모드에서 모든 요소 가시성 확인

---

**REMEMBER: 모든 페이지와 컴포넌트는 반드시 반응형 디자인과 다크모드를 함께 구현해야 합니다!**
**기억하세요: 모바일/태블릿/노트북/데스크탑 모든 화면 크기와 라이트/다크 모드를 지원해야 합니다!**

---



# 디자인 시스템 규칙 (Design System Rules)

## 🎨 CRITICAL: 디자인 시스템 준수

**모든 새로운 페이지와 컴포넌트는 반드시 `docs/design-handoff_D`에 정의된 디자인 시스템을 따라야 합니다.**

참고 문서:
- 디자인 시스템 명세서: `docs/design-handoff_D/00-guides/DESIGN_SYSTEM.md`
- 디자인 적용 계획: `docs/디자인-적용-계획.md`
- 디자인 토큰: `docs/design-handoff_D/01-design-tokens/`

---

## 🎯 CRITICAL: 디자인 시스템 스타일 적용 (2026년 1월 개선사항)

**모든 새로운 페이지와 컴포넌트는 반드시 디자인 시스템 스타일을 따라야 합니다.**

### 핵심 변경사항

#### 1. 페이지 배경
- ✅ **사용**: `bg-gray-50 dark:bg-gray-900`
- ❌ **금지**: `bg-[#F8F9FA]`, `bg-background`, `bg-white` (페이지 배경으로)

#### 2. 카드/컨테이너
- ✅ **모서리**: `rounded-lg` (8px) - **`rounded-2xl` 사용 금지**
- ✅ **테두리**: `border border-gray-100 dark:border-gray-700` - **필수**
- ✅ **그림자**: `shadow-sm dark:shadow-gray-900/30` - **`shadow-md` 사용 금지**
- ✅ **호버 그림자**: `hover:shadow-md dark:hover:shadow-gray-900/50`

#### 3. 버튼
- ✅ **모서리**: `rounded-lg` (8px) - **`rounded-xl` 사용 금지**
- ✅ **그림자**: `shadow-sm hover:shadow-md` - **`shadow-md hover:shadow-lg` 사용 금지**
- ✅ **다크모드**: Primary 버튼은 `dark:bg-emerald-600 dark:hover:bg-emerald-500`

#### 4. 탭 버튼
- ✅ **모서리**: `rounded-t-lg` - **`rounded-t-xl` 사용 금지**
- ✅ **그림자**: 활성 상태에만 `shadow-sm` 추가

#### 5. 업로드 영역
- ✅ **모서리**: `rounded-lg` - **`rounded-xl` 사용 금지**
- ✅ **호버 배경**: `dark:hover:bg-gray-800/50` (투명도 조정)

#### 6. 모달
- ✅ **모서리**: `rounded-lg` - **`rounded-2xl` 사용 금지**
- ✅ **그림자**: `shadow-lg` (모달은 약간 더 강한 그림자 허용)
- ✅ **테두리**: `border border-gray-100 dark:border-gray-700`

### 디자인 시스템 스타일 체크리스트

새로운 페이지를 만들 때 반드시 확인:
- [ ] 페이지 배경: `bg-gray-50 dark:bg-gray-900` 사용
- [ ] 카드: `rounded-lg`, `border border-gray-100`, `shadow-sm` 사용
- [ ] 버튼: `rounded-lg`, `shadow-sm` 사용
- [ ] 탭 버튼: `rounded-t-lg` 사용
- [ ] 업로드 영역: `rounded-lg` 사용
- [ ] 모달: `rounded-lg` 사용
- [ ] 모든 요소에 다크모드 변형 적용

---

## 1. 색상 시스템 (Color System)

### Primary 색상: Emerald Green (#10B981)

**모든 주요 버튼, 강조 요소, 활성 상태에는 Emerald Green을 사용해야 합니다.**

✅ CORRECT:
// Primary 버튼
<button className="bg-emerald-500 hover:bg-emerald-600 active:bg-emerald-700 text-white">
  확인
</button>

// 활성 링크
<Link className="bg-emerald-500 text-white">홈</Link>

// 강조 텍스트
<span className="text-emerald-500 font-semibold">성공</span>❌ INCORRECT:
// Blue 색상 사용 금지 (레거시)
<button className="bg-blue-500">확인</button>
<Link className="bg-blue-500">홈</Link>
### Secondary 색상: Amber Yellow (#FBBF24)

**대기/경고 상태, 보조 강조에만 사용합니다.**

✅ CORRECT:
// 경고 상태
<div className="bg-amber-100 border-l-4 border-amber-400">
  대기중입니다
</div>

// 보조 버튼
<button className="bg-amber-400 hover:bg-amber-500">
  더 알아보기
</button>### Danger 색상: Red (#EF4444)

**오류, 삭제, 위험한 액션에만 사용합니다.**

✅ CORRECT:x
// 삭제 버튼
<button className="bg-red-500 hover:bg-red-600 text-white">
  삭제
</button>

// 에러 메시지
<div className="bg-red-50 border-l-4 border-red-500">
  <p className="text-red-800">오류가 발생했습니다</p>
</div>### Neutral 색상: Gray Scale

**텍스트, 배경, 테두리에 사용합니다.**

✅ CORRECT:x
// 텍스트 계층
<h1 className="text-gray-900">제목</h1>
<p className="text-gray-600">본문</p>
<span className="text-gray-400">보조 텍스트</span>

// 배경
<div className="bg-gray-50">페이지 배경</div>
<div className="bg-white">카드 배경</div>

// 테두리
<input className="border border-gray-300" />---

## 2. 타이포그래피 (Typography)

### 폰트: Pretendard Variable

**모든 텍스트는 Pretendard Variable 폰트를 사용해야 합니다.**

✅ CORRECT:
// 기본적으로 font-sans는 Pretendard로 설정됨
<h1 className="font-sans text-2xl font-bold">제목</h1>
<p className="font-sans text-base">본문</p>### 폰트 크기 계층

**폰트 크기는 다음 계층을 따라야 합니다:**

- `text-xs` (12px): 작은 라벨, 보조 텍스트
- `text-sm` (14px): 테이블 데이터, 폼 라벨
- `text-base` (16px): 본문 텍스트 (기본)
- `text-lg` (18px): 강조 텍스트, 부제목
- `text-xl` (20px): 섹션 제목
- `text-2xl` (24px): 페이지 제목
- `text-3xl` (30px): 메인 헤딩

✅ CORRECT:
<h1 className="text-2xl font-bold leading-tight">페이지 제목</h1>
<h2 className="text-xl font-bold leading-tight">섹션 제목</h2>
<h3 className="text-lg font-semibold">서브섹션 제목</h3>
<p className="text-base">본문 텍스트</p>
<label className="text-sm font-medium">라벨</label>
<span className="text-xs text-gray-500">보조 텍스트</span>---

## 3. 컴포넌트 스타일 (Component Styles)

### 버튼 (Button)

**모든 버튼은 다음 스타일을 따라야 합니다:**

✅ CORRECT:
// Primary 버튼
<button className="
  bg-emerald-500 hover:bg-emerald-600 active:bg-emerald-700
  dark:bg-emerald-600 dark:hover:bg-emerald-500
  text-white font-medium
  px-4 py-2.5 rounded-lg
  shadow-sm hover:shadow-md active:shadow
  transition-all duration-300
  hover:-translate-y-0.5 active:scale-98
">
  확인
</button>

// Secondary 버튼
<button className="
  bg-gray-100 hover:bg-gray-200
  dark:bg-gray-700 dark:hover:bg-gray-600
  text-gray-700 dark:text-gray-200 font-medium
  px-4 py-2.5 rounded-lg
  border border-gray-200 dark:border-gray-600
  transition-colors duration-300
">
  취소
</button>

// Ghost 버튼
<button className="
  bg-transparent hover:bg-emerald-50 dark:hover:bg-emerald-900/20
  text-emerald-500 dark:text-emerald-400 font-medium
  px-4 py-2.5 rounded-lg
  border border-gray-300 dark:border-gray-600
  transition-colors duration-300
">
  더보기
</button>

// Danger 버튼
<button className="
  bg-red-500 hover:bg-red-600
  dark:bg-red-600 dark:hover:bg-red-500
  text-white font-medium
  px-4 py-2.5 rounded-lg
  shadow-sm hover:shadow-md
  transition-all duration-300
">
  삭제
</button>**버튼 규칙:**
- 모서리: `rounded-lg` (8px) 사용 
- 트랜지션: `transition-all duration-300` 필수
- 그림자: `shadow-sm hover:shadow-md` (미묘한 그림자)
- 호버 효과: `hover:-translate-y-0.5` 또는 `hover:shadow-md`
- 액티브 효과: `active:scale-98`
- 다크모드: Primary 버튼은 `dark:bg-emerald-600 dark:hover:bg-emerald-500`

### 카드 (Card)

**모든 카드는 다음 스타일을 따라야 합니다:**

✅ CORRECT:
// 기본 카드 
<div className="
  bg-white dark:bg-gray-800
  border border-gray-100 dark:border-gray-700
  rounded-lg
  shadow-sm dark:shadow-gray-900/30
  hover:shadow-md dark:hover:shadow-gray-900/50
  p-6
  transition-all duration-300 hover:-translate-y-1
">
  <h3 className="text-lg font-semibold mb-2 text-gray-900 dark:text-gray-100">카드 제목</h3>
  <p className="text-gray-600 dark:text-gray-400">카드 내용</p>
</div>

// 3D 효과 카드 (특별한 경우)
<div className="
  bg-white dark:bg-gray-800
  border border-gray-100 dark:border-gray-700
  rounded-lg
  shadow-[0_20px_50px_rgba(16,185,129,0.15)]
  dark:shadow-[0_20px_50px_rgba(16,185,129,0.1)]
  hover:shadow-[0_20px_50px_rgba(16,185,129,0.2)]
  dark:hover:shadow-[0_20px_50px_rgba(16,185,129,0.15)]
  p-6
  transition-all duration-300 hover:-translate-y-2
">
  프리미엄 카드
</div>**카드 규칙:**
- 모서리: `rounded-lg` (8px) 사용 
- 테두리: `border border-gray-100 dark:border-gray-700` 필수
- 패딩: `p-6` (24px) 기본
- 그림자: `shadow-sm dark:shadow-gray-900/30` 기본, 호버 시 `shadow-md dark:hover:shadow-gray-900/50`
- 호버 효과: `hover:-translate-y-1` 필수
- 다크모드: 배경 `dark:bg-gray-800`, 텍스트 `dark:text-gray-100`

### 입력 필드 (Input)

**모든 입력 필드는 다음 스타일을 따라야 합니다:**

✅ CORRECT:
// 기본 입력 필드
<input
  type="text"
  className="
    w-full px-4 py-2.5
    border border-gray-300 rounded-lg
    text-gray-900 text-base
    focus:outline-none
    focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2
    placeholder:text-gray-400
    transition-colors duration-150
    disabled:bg-gray-100 disabled:cursor-not-allowed
  "
/>

// 에러 상태
<input
  type="text"
  className="
    w-full px-4 py-2.5
    border-2 border-red-500 rounded-lg
    focus:ring-2 focus:ring-red-500 focus:ring-offset-2
  "
/>**입력 필드 규칙:**
- 모서리: `rounded-lg` (8px)
- 패딩: `px-4 py-2.5`
- 포커스 링: `focus:ring-2 focus:ring-emerald-500 focus:ring-offset-2`
- 에러 상태: `border-red-500`

### 폼 (Form)

✅ CORRECT:
<div className="space-y-4">
  <div className="space-y-2">
    <label className="text-sm font-medium text-gray-900 dark:text-gray-100">
      라벨
    </label>
    <input className="w-full px-4 py-2.5 border border-gray-300 dark:border-gray-600 rounded-lg bg-white dark:bg-gray-800 text-gray-900 dark:text-gray-100" />
    <p className="text-xs text-gray-500 dark:text-gray-400">도움말 텍스트</p>
  </div>
</div>**폼 규칙:**
- 라벨: `text-sm font-medium text-gray-900 dark:text-gray-100`
- 필드 간격: `gap-4` (16px)
- 섹션 간격: `gap-8` (32px)

### 탭 버튼 (Tab Button)

**모든 탭 버튼은 다음 스타일을 따라야 합니다:**

✅ CORRECT:
// 탭 메뉴
<div className="flex gap-2 border-b border-gray-200 dark:border-gray-700">
  <button className={`
    px-4 py-2.5 font-medium rounded-t-lg transition-all duration-300
    ${activeTab === 'tab1'
      ? 'bg-emerald-500 dark:bg-emerald-600 text-white shadow-sm'
      : 'bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 hover:bg-gray-200 dark:hover:bg-gray-600'
    }
  `}>
    탭 1
  </button>
</div>**탭 버튼 규칙:**
- 모서리: `rounded-t-lg` 
- 활성 상태: `bg-emerald-500 dark:bg-emerald-600 text-white shadow-sm`
- 비활성 상태: `bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300`
- 호버: `hover:bg-gray-200 dark:hover:bg-gray-600`

### 업로드 영역 (Upload Area)

**모든 업로드 영역은 다음 스타일을 따라야 합니다:**

✅ CORRECT:
<div className="
  border-2 border-dashed
  border-gray-300 dark:border-gray-600
  rounded-lg
  p-8
  text-center
  cursor-pointer
  transition-all duration-300
  hover:border-emerald-500 dark:hover:border-emerald-400
  hover:bg-emerald-50 dark:hover:bg-gray-800/50
">
  <Upload className="w-12 h-12 mx-auto mb-4 text-gray-400 dark:text-gray-500" />
  <p className="text-sm text-gray-600 dark:text-gray-300">
    파일을 드래그하거나 클릭하여 업로드
  </p>
</div>**업로드 영역 규칙:**
- 모서리: `rounded-lg` 
- 테두리: `border-2 border-dashed border-gray-300 dark:border-gray-600`
- 호버: `hover:border-emerald-500 dark:hover:border-emerald-400`
- 배경: `hover:bg-emerald-50 dark:hover:bg-gray-800/50`

### 모달 (Modal)

**모든 모달은 다음 스타일을 따라야 합니다:**

✅ CORRECT:
// 모달 오버레이
<div className="
  fixed inset-0 z-50
  flex items-center justify-center
  bg-black/50 dark:bg-black/70
  backdrop-blur-sm
  p-4
">
  {/* 모달 컨텐츠 */}
  <div className="
    relative
    bg-white dark:bg-gray-800
    rounded-lg
    p-6 sm:p-8
    max-w-md w-full
    shadow-lg
    border border-gray-100 dark:border-gray-700
  ">
    {/* 모달 내용 */}
  </div>
</div>**모달 규칙:**
- 모서리: `rounded-lg` 
- 배경: `bg-white dark:bg-gray-800`
- 테두리: `border border-gray-100 dark:border-gray-700`
- 그림자: `shadow-lg` (모달은 약간 더 강한 그림자)
- 오버레이: `bg-black/50 dark:bg-black/70 backdrop-blur-sm`

---

## 4. 간격 시스템 (Spacing System)

**모든 간격은 4px 단위를 사용합니다 (Tailwind 기본 시스템).**

✅ CORRECT:x
// 패딩
<div className="p-4">16px</div>
<div className="px-4 py-2">좌우 16px, 위아래 8px</div>

// 마진
<div className="mt-4 mb-6">위 16px, 아래 24px</div>

// Gap
<div className="flex gap-4">자식 요소 간 16px</div>
<div className="grid gap-6">그리드 아이템 간 24px</div>**표준 간격:**
- 버튼 패딩: `px-4 py-2.5` (기본)
- 카드 패딩: `p-6` (24px)
- 폼 필드 간격: `gap-4` (16px)
- 섹션 간격: `gap-8` (32px)

---

## 5. 그림자 & 효과 (Shadows & Effects)

**그림자와 효과는 다음 규칙을 따라야 합니다:**

✅ CORRECT:
// 기본 카드 그림자 
<div className="shadow-sm dark:shadow-gray-900/30">기본 카드</div>

// 호버 카드 그림자
<div className="shadow-sm dark:shadow-gray-900/30 hover:shadow-md dark:hover:shadow-gray-900/50">호버 효과</div>

// 3D 효과 (특별한 경우)
<div className="shadow-[0_20px_50px_rgba(16,185,129,0.15)]">
  3D 카드
</div>

// 트랜지션
<div className="transition-all duration-300">부드러운 전환</div>**트랜지션 규칙:**
- 기본: `transition-all duration-300`
- 색상만: `transition-colors duration-300`
- 빠른 전환: `duration-150` (클릭 등)

---

## 6. 인터랙션 & 애니메이션 (Interactions & Animations)

**모든 인터랙티브 요소는 다음 효과를 포함해야 합니다:**

✅ CORRECT:
// 버튼 호버 
<button className="
  hover:bg-emerald-600
  dark:hover:bg-emerald-500
  hover:shadow-md
  hover:-translate-y-0.5
  transition-all duration-300
">
  버튼
</button>

// 카드 호버 
<div className="
  hover:shadow-md
  dark:hover:shadow-gray-900/50
  hover:-translate-y-1
  transition-all duration-300
">
  카드
</div>

// 포커스 링
<input className="
  focus:ring-2
  focus:ring-emerald-500
  focus:ring-offset-2
  transition-colors duration-150
" />**인터랙션 규칙:**
- 호버: 색상 변경 + 그림자 강화 + 약간의 이동
- 포커스: 링 효과 (`ring-2 ring-emerald-500`)
- 액티브: `active:scale-98` (버튼 클릭 시)

---

## 7. 반응형 디자인 (Responsive Design)

**모든 레이아웃은 Mobile First 접근 방식을 사용해야 합니다.**

✅ CORRECT:
// Mobile First 방식
<div className="
  grid grid-cols-1          // 모바일: 1열
  md:grid-cols-2            // 태블릿: 2열
  lg:grid-cols-3            // 데스크톱: 3열
  gap-4                     // 기본 간격
">
  <div>카드 1</div>
  <div>카드 2</div>
  <div>카드 3</div>
</div>

// 반응형 타이포그래피
<h1 className="
  text-xl              // 모바일: 20px
  sm:text-2xl          // 태블릿: 24px
  lg:text-3xl          // 데스크톱: 30px
  font-bold
">
  제목
</h1>

// 반응형 패딩
<div className="
  p-4                  // 모바일: 16px
  lg:p-8               // 데스크톱: 32px
">
  컨텐츠
</div>
**반응형 규칙:**
- Mobile First: 작은 화면부터 큰 화면 순서로 작성
- 브레이크포인트: `sm:` (640px), `md:` (768px), `lg:` (1024px), `xl:` (1280px)
- 터치 영역: 최소 44x44px (모바일)
- 텍스트 크기: 최소 14px (모바일)

---

## 8. 레이아웃 (Layout)

**페이지 레이아웃은 다음 규칙을 따라야 합니다:**

✅ CORRECT:
// 페이지 컨테이너
<div className="
  min-h-screen
  bg-gray-50              // 페이지 배경
  p-4 lg:p-8              // 반응형 패딩
">
  <div className="
    max-w-4xl              // 최대 너비
    mx-auto                // 중앙 정렬
  ">
    {/* 컨텐츠 */}
  </div>
</div>

// 카드 그리드
<div className="
  grid grid-cols-1
  md:grid-cols-2
  lg:grid-cols-3
  gap-6
">
  {/* 카드들 */}
</div>**레이아웃 규칙:**
- 페이지 배경: **반드시** `bg-gray-50 dark:bg-gray-900` 사용 
  - ❌ 금지: `bg-[#F8F9FA]`, `bg-background`, `bg-white` (페이지 배경으로)
- 카드 배경: `bg-white dark:bg-gray-800`
- 카드 테두리: `border border-gray-100 dark:border-gray-700` (필수)
- 컨테이너 최대 너비: `max-w-4xl` 또는 `max-w-6xl`
- 중앙 정렬: `mx-auto`

---

## 9. 아이콘 (Icons)

**아이콘은 Lucide React를 사용해야 합니다.**

✅ CORRECT:
import { Home, Search, User, Settings } from 'lucide-react';

// 버튼 내 아이콘 (20px)
<button className="flex items-center gap-2">
  <Home size={20} />
  홈
</button>

// 네비게이션 아이콘 (24px)
<Link className="flex items-center gap-2">
  <Settings size={24} />
  설정
</Link>**아이콘 규칙:**
- 라이브러리: `lucide-react` 사용
- 크기: `size={20}` (버튼), `size={24}` (네비게이션)
- 색상: `text-emerald-500` (Primary), `text-gray-600` (기본)

---

## 10. 상태 표시 (Status Indicators)

**상태 표시는 다음 색상을 사용해야 합니다:**

✅ CORRECT:
// 성공/완료 상태
<span className="bg-emerald-100 text-emerald-800 px-3 py-1 rounded-full text-xs font-semibold">
  완료
</span>

// 대기/경고 상태
<span className="bg-amber-100 text-amber-800 px-3 py-1 rounded-full text-xs font-semibold">
  대기중
</span>

// 오류 상태
<span className="bg-red-100 text-red-800 px-3 py-1 rounded-full text-xs font-semibold">
  오류
</span>

// 정보 상태
<span className="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-xs font-semibold">
  정보
</span>---

## 11. 금지 사항 (Prohibited)

**다음은 절대 사용하지 마세요:**

❌ 금지:
- Blue 색상을 Primary로 사용 (`bg-blue-500`, `text-blue-500` 등)
- Geist 폰트 사용 (Pretendard로 변경 완료)
- `bg-[#F8F9FA]` 사용 (반드시 `bg-gray-50 dark:bg-gray-900` 사용)
- `rounded-xl`, `rounded-2xl` 사용 (카드/버튼은 `rounded-lg` 사용)
- `shadow-md` 사용 (반드시 `shadow-sm` 사용, 다크모드 `dark:shadow-gray-900/30`)
- 테두리 없는 카드 (반드시 `border border-gray-100 dark:border-gray-700` 추가)
- 트랜지션 없이 호버 효과 사용
- 모바일 First가 아닌 Desktop First 접근
- 일관성 없는 간격 사용 (4px 단위 준수)

---

## 12. 체크리스트 (Checklist)

**새로운 컴포넌트나 페이지를 만들 때 다음을 확인하세요:**

- [ ] Primary 색상으로 Emerald Green (#10B981) 사용했는가?
- [ ] Pretendard Variable 폰트를 사용했는가?
- [ ] 페이지 배경에 `bg-gray-50 dark:bg-gray-900`를 적용했는가? (`bg-[#F8F9FA]` 사용 금지)
- [ ] 버튼에 `rounded-lg`와 `shadow-sm`을 적용했는가? 
- [ ] 카드에 `rounded-lg`, `border border-gray-100 dark:border-gray-700`, `shadow-sm dark:shadow-gray-900/30`을 적용했는가? 
- [ ] 입력 필드에 포커스 링 (`focus:ring-emerald-500 dark:focus:ring-emerald-400`)을 적용했는가?
- [ ] 탭 버튼에 `rounded-t-lg`와 디자인 시스템 스타일을 적용했는가?
- [ ] 업로드 영역에 `rounded-lg`를 적용했는가?
- [ ] 모달에 `rounded-lg`와 적절한 테두리를 적용했는가?
- [ ] Mobile First 방식으로 반응형을 구현했는가?
- [ ] 모든 인터랙티브 요소에 트랜지션을 추가했는가?
- [ ] 간격을 4px 단위로 일관되게 사용했는가?
- [ ] 그림자 효과를 디자인 시스템 스타일 (`shadow-sm`)로 적용했는가?
- [ ] 다크모드에서 모든 요소가 올바르게 표시되는가?
- [ ] 접근성을 고려했는가? (색상 대비, 키보드 네비게이션 등)

---

## 13. 참고 자료

**디자인 시스템 관련 문서:**
- 디자인 시스템 명세서: `docs/design-handoff_D/00-guides/DESIGN_SYSTEM.md`
- 개발자 가이드: `docs/design-handoff_D/00-guides/DEVELOPER_GUIDE.md`
- 디자인 적용 계획: `docs/디자인-적용-계획.md`
- 색상 토큰: `docs/design-handoff_D/01-design-tokens/colors.json`
- 타이포그래피: `docs/design-handoff_D/01-design-tokens/typography.json`
- 간격 시스템: `docs/design-handoff_D/01-design-tokens/spacing.json`
- 그림자 & 효과: `docs/design-handoff_D/01-design-tokens/shadows.json`

**컴포넌트 예시:**
- 버튼: `docs/design-handoff_D/02-components/buttons/code.tsx`
- 카드: `docs/design-handoff_D/02-components/cards/code.tsx`
- 폼: `docs/design-handoff_D/02-components/forms/code.tsx`

---

**REMEMBER: Always follow the design system rules when creating new components or pages!**
**기억하세요: 새로운 컴포넌트나 페이지를 만들 때 항상 디자인 시스템 규칙을 따라야 합니다!**

REMEMBER: 프로덕션 환경에서는 디버그 로그를 절대 출력하지 마세요!
기억하세요: 배포 환경의 콘솔은 깨끗해야 합니다!