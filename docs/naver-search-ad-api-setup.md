# 네이버 검색광고 API 설정 가이드

네이버 검색광고 API를 사용하여 키워드의 공식 검색량을 조회하는 기능을 사용하기 위한 설정 가이드입니다.

## 1단계: 네이버 검색광고 API 권한 획득

### 필요한 정보
네이버 검색광고 API를 사용하기 위해서는 다음 3가지 정보가 필요합니다:

1. **CUSTOMER_ID** (고유 고객 아이디)
2. **액세스 라이선스** (API Key)
3. **비밀키** (Secret Key)

### 발급 방법
1. 네이버 검색광고에 로그인합니다
2. 하단 '광고시스템' 메뉴를 클릭합니다
3. 상단 메뉴에서 **[도구 > API 사용 관리]**로 이동합니다
4. API 사용 신청을 완료하고 다음 정보를 확인합니다:
   - CUSTOMER_ID
   - 액세스 라이선스 (API Key)
   - 비밀키 (Secret Key)

> ⚠️ **주의**: 이 정보는 보안상 중요하므로 메모장에 따로 저장해 두시기 바랍니다.

---

## 2단계: 환경 변수 설정

프로젝트 루트 디렉토리에 `.env.local` 파일을 생성하거나 수정하여 다음 환경 변수를 추가합니다:

```env
# 네이버 검색광고 API 인증 정보
NAVER_CUSTOMER_ID=your_customer_id_here
NAVER_ACCESS_LICENSE=your_access_license_here
NAVER_SECRET_KEY=your_secret_key_here
```

### 예시
```env
NAVER_CUSTOMER_ID=12345678
NAVER_ACCESS_LICENSE=01IW0000N0000000000
NAVER_SECRET_KEY=your_secret_key_string_here
```

### 중요 사항
- `.env.local` 파일은 Git에 커밋하지 마세요 (이미 `.gitignore`에 포함되어 있습니다)
- 환경 변수 값에 따옴표(`"` 또는 `'`)를 사용하지 마세요
- 서버를 재시작해야 환경 변수가 적용됩니다

---

## 3단계: 서버 재시작

환경 변수를 설정한 후 개발 서버를 재시작합니다:

```bash
# 개발 서버 중지 (Ctrl + C)
# 그 다음 다시 시작
pnpm run dev
```

---

## 4단계: 기능 사용

1. 키워드 분석 페이지(`/tools/keyword-analysis`)로 이동합니다
2. 키워드를 입력합니다
3. **"🔍 네이버 검색량"** 버튼을 클릭합니다
4. 네이버 검색광고 API에서 조회한 공식 검색량이 표시됩니다:
   - 총 검색량 (PC + 모바일 합산)
   - PC 검색량
   - 모바일 검색량

---

## 문제 해결

### 환경 변수가 설정되지 않았다는 오류가 발생하는 경우

1. `.env.local` 파일이 프로젝트 루트에 있는지 확인합니다
2. 환경 변수 이름이 정확한지 확인합니다:
   - `NAVER_CUSTOMER_ID`
   - `NAVER_ACCESS_LICENSE`
   - `NAVER_SECRET_KEY`
3. 환경 변수 값에 따옴표가 없는지 확인합니다
4. 서버를 재시작했는지 확인합니다

### API 호출 오류가 발생하는 경우

1. 네이버 검색광고 API 사용 권한이 활성화되어 있는지 확인합니다
2. CUSTOMER_ID, 액세스 라이선스, 비밀키가 올바른지 확인합니다
3. 네이버 검색광고 API 서비스 상태를 확인합니다
4. 브라우저 개발자 도구의 콘솔과 네트워크 탭에서 오류 메시지를 확인합니다

### 검색 결과가 없는 경우

- 키워드의 검색량이 너무 적거나 데이터가 없는 경우 "검색량이 너무 적거나 데이터가 없습니다" 메시지가 표시됩니다
- 이는 정상적인 응답이며, 다른 키워드로 시도해보세요

---

## API 엔드포인트

네이버 검색광고 API는 서버 사이드에서만 호출됩니다:

- **API Route**: `/api/naver-keyword-search`
- **메서드**: POST
- **요청 본문**: `{ "keyword": "키워드" }`
- **응답**: 
  ```json
  {
    "success": true,
    "data": {
      "keyword": "키워드",
      "pcSearchVolume": 1000,
      "mobileSearchVolume": 2000,
      "totalSearchVolume": 3000
    }
  }
  ```

---

## 보안 주의사항

- ⚠️ **절대 클라이언트 사이드에서 네이버 API 인증 정보를 사용하지 마세요**
- ⚠️ **환경 변수는 서버 사이드에서만 접근 가능합니다**
- ⚠️ **`.env.local` 파일을 Git에 커밋하지 마세요**
- ⚠️ **프로덕션 환경에서는 환경 변수를 안전하게 관리하세요**

---

## 참고 자료

- [네이버 검색광고 API 공식 문서](https://naver.github.io/searchad-apidoc/)
- [네이버 검색광고 API 사용 가이드](https://help.naver.com/support/contents/contents.nhn?serviceNo=116&categoryNo=2642)

