# 프로젝트 기술 문서

> 마케팅 도구 모음 프로젝트의 기술적인 사항을 정리한 문서  
> 작성일: 2026년 1월  
> 목적: 비슷한 프로젝트 진행 시 개발을 쉽게 진행하기 위한 참고 자료

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [프로젝트 구조](#프로젝트-구조)
4. [주요 설정 파일](#주요-설정-파일)
5. [환경 변수 설정](#환경-변수-설정)
6. [주요 기능 및 통합 서비스](#주요-기능-및-통합-서비스)
7. [아키텍처 패턴](#아키텍처-패턴)
8. [개발 가이드](#개발-가이드)
9. [배포 및 운영](#배포-및-운영)
10. [문제 해결 가이드](#문제-해결-가이드)

---

## 프로젝트 개요

### 프로젝트명

**Draiger (드라이거) - 데일리 툴킷**

### 프로젝트 목적

마케팅, 트렌드, 일상의 유용한 정보가 매일 자동으로 업데이트되는 블로그 콘텐츠와 마케팅 도구를 제공하는 웹 애플리케이션

### 주요 기능

- **블로그/인사이트**: Notion 기반 CMS로 콘텐츠 관리
- **마케팅 도구**: 광고 성과 분석, ROI 계산, 키워드 분석 등 다양한 계산 도구
- **AI 기능**: Google Gemini API를 활용한 자동 추정 및 분석
- **PWA 지원**: 오프라인 지원 및 앱 설치 기능

---

## 기술 스택

### 프론트엔드

#### 핵심 프레임워크

- **Next.js 15.5.9** (App Router)
  - Turbopack 사용 (`--turbopack` 플래그)
  - 서버 컴포넌트 및 클라이언트 컴포넌트 분리
  - API Routes를 통한 백엔드 기능 제공

#### 언어 및 타입 시스템

- **TypeScript 5.x**
  - 엄격한 타입 체크 (`strict: true`)
  - 모듈 해석: `bundler` 방식
  - 경로 별칭: `@/*` → `./src/*`

#### 스타일링

- **Tailwind CSS 4**
  - PostCSS 플러그인 사용 (`@tailwindcss/postcss`)
  - 다크모드 지원 (`next-themes`와 통합)
  - 커스텀 CSS 변수 및 테마 설정

#### UI 라이브러리

- **Lucide React** - 아이콘
- **Framer Motion** - 애니메이션
- **Recharts** - 데이터 시각화 (차트)
- **React Markdown** - 마크다운 렌더링
- **React Syntax Highlighter** - 코드 하이라이팅

#### 상태 관리

- **React Hooks** (useState, useEffect 등)
- **LocalStorage** - 클라이언트 사이드 데이터 저장
- **next-themes** - 테마(다크모드) 상태 관리

### 백엔드/API

#### API 프레임워크

- **Next.js API Routes**
  - Route Handlers (`route.ts`)
  - 서버 사이드 전용 (환경 변수 접근 가능)

#### 외부 API 통합

- **Google Gemini AI** (`@google/generative-ai`)

  - 상품 정보 자동 추정
  - 광고 성과 분석
  - 키워드 추천

- **Notion API** (`@notionhq/client`, `notion-to-md`)

  - CMS로 사용
  - 블로그 포스트 관리
  - 마크다운 변환

- **네이버 검색광고 API**
  - 키워드 검색량 조회
  - 공식 API 인증 (서명 생성)

#### 이메일

- **Nodemailer**
  - 문의하기 기능
  - SMTP 서버 연동

### 파일 처리

#### 이미지 처리

- **Sharp** - 이미지 리사이징 및 최적화
- **html2canvas** - HTML을 이미지로 변환
- **jsPDF** - PDF 생성

#### 문서 처리

- **mammoth** - Word 문서 처리
- **xlsx** - Excel 파일 처리
- **pdfjs-dist** - PDF 뷰어
- **pptxjs** - PowerPoint 처리
- **jszip** - ZIP 파일 처리

### 기타 라이브러리

#### 유틸리티

- **clsx** - 조건부 클래스명
- **tailwind-merge** - Tailwind 클래스 병합
- **lenis** - 부드러운 스크롤

#### 댓글 시스템

- **@giscus/react** - GitHub 기반 댓글 시스템

#### PWA

- **Service Worker** (커스텀 구현)
- **Web Manifest** (`site.webmanifest`)

---

## 프로젝트 구조

```
adtoolkit/
├── src/
│   ├── app/                          # Next.js App Router
│   │   ├── (main)/                   # 메인 레이아웃 그룹
│   │   ├── api/                      # API Routes
│   │   │   ├── analyze-*/            # 분석 API
│   │   │   ├── estimate-*/           # 추정 API
│   │   │   ├── compress/             # 이미지 압축
│   │   │   ├── resize/               # 이미지 리사이즈
│   │   │   ├── convert-hwp/         # HWP 변환
│   │   │   ├── contact/              # 문의하기
│   │   │   ├── naver-keyword-search/ # 네이버 키워드 검색
│   │   │   └── tistory/              # 티스토리 연동
│   │   ├── tools/                    # 도구 페이지들
│   │   │   ├── ad-performance/       # 광고 성과 계산
│   │   │   ├── keyword-analysis/     # 키워드 분석
│   │   │   ├── roi-calculator/       # ROI 계산기
│   │   │   ├── break-even-point/     # 손익분기점
│   │   │   ├── budget-calculator/    # 예산 계산기
│   │   │   ├── profitability-diagnosis/ # 수익성 진단
│   │   │   ├── conversion-calculator/   # CRO 계산기
│   │   │   ├── image-resize/         # 이미지 리사이즈
│   │   │   ├── file-preview/         # 파일 미리보기
│   │   │   └── ...                   # 기타 도구들
│   │   ├── insight/                  # 블로그/인사이트
│   │   │   ├── [slug]/               # 포스트 상세
│   │   │   └── category/[category]/  # 카테고리별 목록
│   │   ├── about/                    # 소개 페이지
│   │   ├── contact/                  # 문의하기 페이지
│   │   ├── privacy/                  # 개인정보 처리방침
│   │   ├── terms/                    # 이용약관
│   │   ├── support/                  # 후원하기
│   │   ├── search/                   # 검색 페이지
│   │   ├── layout.tsx                # 루트 레이아웃
│   │   ├── page.tsx                  # 홈페이지
│   │   ├── globals.css               # 전역 스타일
│   │   ├── robots.ts                # robots.txt
│   │   └── sitemap.ts               # sitemap.xml
│   ├── components/                   # React 컴포넌트
│   │   ├── ui/                       # 기본 UI 컴포넌트
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Input.tsx
│   │   │   └── Select.tsx
│   │   ├── file-preview/             # 파일 미리보기 컴포넌트
│   │   ├── Header.tsx                # 헤더
│   │   ├── Footer.tsx                # 푸터
│   │   ├── Navbar.tsx                # 네비게이션
│   │   ├── ThemeProvider.tsx         # 테마 프로바이더
│   │   ├── PWAServiceWorker.tsx      # PWA 서비스 워커
│   │   ├── PWAInstallPrompt.tsx      # PWA 설치 프롬프트
│   │   └── ...                       # 기타 컴포넌트
│   ├── lib/                          # 라이브러리/유틸리티
│   │   └── notion.ts                 # Notion API 클라이언트
│   └── utils/                        # 유틸리티 함수
│       ├── date-format.ts            # 날짜 포맷팅
│       └── markdown-renderer.ts      # 마크다운 렌더링
├── public/                           # 정적 파일
│   ├── service-worker.js             # PWA 서비스 워커
│   ├── site.webmanifest              # PWA 매니페스트
│   ├── *.png, *.jpg, *.ico          # 이미지 및 아이콘
│   └── ...
├── docs/                             # 문서
├── scripts/                          # 스크립트
│   ├── convert_hwp.py               # HWP 변환 스크립트
│   └── generate_ico.py              # 아이콘 생성 스크립트
├── next.config.ts                    # Next.js 설정
├── tsconfig.json                     # TypeScript 설정
├── postcss.config.mjs               # PostCSS 설정
├── eslint.config.mjs                # ESLint 설정
├── package.json                      # 패키지 의존성
└── pnpm-lock.yaml                    # 패키지 잠금 파일
```

---

## 주요 설정 파일

### next.config.ts

```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // 모든 HTTPS 호스트 허용 (외부 이미지)
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
      {
        protocol: "http",
        hostname: "**",
      },
    ],
    unoptimized: false,
  },
  // 캐시 헤더 설정
  async headers() {
    return [
      {
        source: "/_next/static/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
      {
        source: "/:path*\\.(ico|png|jpg|jpeg|svg|webp|woff|woff2|ttf|eot)",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=86400, must-revalidate",
          },
        ],
      },
      {
        source: "/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, s-maxage=10, stale-while-revalidate=59",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
```

**주요 설정:**

- 이미지 최적화: 외부 이미지 호스트 허용
- 캐시 전략: 정적 파일은 장기 캐시, 페이지는 짧은 캐시 + stale-while-revalidate

### tsconfig.json

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**주요 설정:**

- 엄격한 타입 체크 (`strict: true`)
- 경로 별칭: `@/*` → `./src/*`
- 모듈 해석: `bundler` (Next.js에 최적화)

### postcss.config.mjs

```javascript
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
```

**설명:**

- Tailwind CSS 4의 새로운 PostCSS 플러그인 사용

### package.json 스크립트

```json
{
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack",
    "start": "next start",
    "lint": "eslint"
  }
}
```

**주요 스크립트:**

- `dev`: 개발 서버 실행 (Turbopack 사용)
- `build`: 프로덕션 빌드 (Turbopack 사용)
- `start`: 프로덕션 서버 실행
- `lint`: ESLint 실행

---

## 환경 변수 설정

### 필수 환경 변수

프로젝트 루트에 `.env.local` 파일을 생성하고 다음 변수들을 설정해야 합니다:

```env
# Google Gemini AI API
GEMINI_API_KEY=your_gemini_api_key_here

# Notion API (블로그 CMS)
NOTION_API_KEY=your_notion_api_key_here
NOTION_DATABASE_ID=your_notion_database_id_here

# 네이버 검색광고 API (키워드 분석)
NAVER_CUSTOMER_ID=your_customer_id_here
NAVER_ACCESS_LICENSE=your_access_license_here
NAVER_SECRET_KEY=your_secret_key_here

# 이메일 설정 (문의하기 기능)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_FROM=your_email@gmail.com
SMTP_TO=recipient_email@gmail.com

# 사이트 URL (SEO 및 메타데이터)
NEXT_PUBLIC_SITE_URL=https://adtoolkit.kr
```

### 환경 변수 설명

#### Google Gemini API

- **용도**: AI 기반 상품 정보 추정, 광고 성과 분석
- **발급 방법**: [Google AI Studio](https://aistudio.google.com/app/apikey)
- **보안**: 서버 사이드 전용 (클라이언트 노출 금지)

#### Notion API

- **용도**: 블로그 CMS로 사용
- **발급 방법**:
  1. [Notion Developers](https://www.notion.so/my-integrations)에서 통합 생성
  2. API 키 발급
  3. 데이터베이스에 통합 연결
  4. 데이터베이스 ID 복사 (URL에서 확인)
- **보안**: 서버 사이드 전용

#### 네이버 검색광고 API

- **용도**: 키워드 검색량 조회
- **발급 방법**: [네이버 검색광고 API 설정 가이드](./naver-search-ad-api-setup.md) 참고
- **보안**: 서버 사이드 전용, 서명 생성 필수

#### 이메일 설정 (SMTP)

- **용도**: 문의하기 기능
- **Gmail 사용 시**: 앱 비밀번호 필요
- **보안**: 서버 사이드 전용

### 환경 변수 접근 방법

#### 서버 사이드 (API Routes)

```typescript
// 서버 사이드에서 직접 접근 가능
const apiKey = process.env.GEMINI_API_KEY;
```

#### 클라이언트 사이드

```typescript
// NEXT_PUBLIC_ 접두사가 있는 변수만 접근 가능
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL;
```

**주의사항:**

- `NEXT_PUBLIC_` 접두사가 없는 변수는 클라이언트에서 접근 불가
- API 키 등 민감한 정보는 절대 클라이언트에 노출하지 않기
- `.env.local` 파일은 Git에 커밋하지 않기 (`.gitignore`에 포함됨)

---

## 주요 기능 및 통합 서비스

### 1. 블로그/인사이트 (Notion CMS)

#### 구조

- **데이터 소스**: Notion 데이터베이스
- **변환**: Notion → Markdown → React 컴포넌트
- **페이지네이션**: 지원 (12개씩)
- **카테고리 필터링**: 지원

#### 주요 함수 (`src/lib/notion.ts`)

```typescript
// Published 게시글 조회
getPublishedPosts(): Promise<Post[]>

// 페이지네이션 지원 조회
getPublishedPostsPaginated(page: number, pageSize: number): Promise<PaginatedPosts>

// 카테고리별 조회
getPublishedPostsByCategory(category: string): Promise<Post[]>

// Slug로 특정 게시글 조회
getPostBySlug(slug: string): Promise<Post | null>

// 게시글 콘텐츠 가져오기 (마크다운 변환)
getPostContent(pageId: string): Promise<string>
```

#### Notion 데이터베이스 스키마

필수 속성:

- `title` (제목)
- `slug` (URL 슬러그)
- `metaDescription` (메타 설명)
- `Published` (체크박스, 발행 여부)
- `blogPost` (텍스트, 요약)
- `category` (텍스트, 선택사항)
- `date` (날짜, 선택사항)
- `tags` (다중 선택, 선택사항)

### 2. 마케팅 도구

#### 광고 성과 계산 도구 (`/tools/ad-performance`)

- 여러 상품의 광고 성과 비교
- AI 기반 상품 정보 자동 추정
- 차트 시각화 (Recharts)

#### 키워드 분석 도구 (`/tools/keyword-analysis`)

- 네이버 검색광고 API 연동
- 키워드 검색량 조회
- AI 기반 키워드 추천

#### ROI 계산기 (`/tools/roi-calculator`)

- 투자 대비 수익률 계산
- AI 기반 ROI 분석

#### 기타 도구들

- 손익분기점 계산기
- 광고 예산 계산기
- CRO 전환율 최적화 계산기
- 수익성 진단 도구
- 이미지 리사이즈
- 파일 미리보기
- QR 코드 생성기
- URL 단축기
- 등등...

### 3. AI 기능 (Google Gemini)

#### 사용 위치

- 상품 정보 자동 추정
- 광고 성과 분석
- 키워드 추천
- ROI 분석
- 등등...

#### API 엔드포인트 예시

```typescript
// src/app/api/analyze-performance/route.ts
export async function POST(request: NextRequest) {
  const apiKey = process.env.GEMINI_API_KEY;
  const genAI = new GoogleGenerativeAI(apiKey);
  const model = genAI.getGenerativeModel({ model: "gemini-pro" });

  // 프롬프트 생성 및 AI 호출
  const result = await model.generateContent(prompt);
  // ...
}
```

### 4. PWA (Progressive Web App)

#### 구현 내용

- **Service Worker**: 오프라인 캐싱
- **Web Manifest**: 앱 설치 지원
- **설치 프롬프트**: 사용자에게 설치 안내

#### 파일 위치

- `public/service-worker.js`: 서비스 워커 스크립트
- `public/site.webmanifest`: 매니페스트 파일
- `src/components/PWAServiceWorker.tsx`: 서비스 워커 등록
- `src/components/PWAInstallPrompt.tsx`: 설치 프롬프트

#### 주요 기능

- 정적 리소스 캐싱
- 오프라인 지원
- 앱 설치 (홈 화면에 추가)
- 업데이트 자동 감지

### 5. 다크모드

#### 구현 방식

- **next-themes** 라이브러리 사용
- 클래스 기반 다크모드 (`class` 속성)
- 시스템 테마 감지 지원

#### 설정 위치

- `src/components/ThemeProvider.tsx`: 테마 프로바이더
- `src/app/layout.tsx`: 루트 레이아웃에 적용
- `src/app/globals.css`: 다크모드 CSS 변수

### 6. SEO 최적화

#### 구현 내용

- **메타데이터**: Next.js Metadata API 사용
- **구조화된 데이터**: JSON-LD 스키마
- **robots.txt**: `src/app/robots.ts`
- **sitemap.xml**: `src/app/sitemap.ts`
- **Open Graph**: 소셜 미디어 공유 최적화
- **Twitter Cards**: Twitter 공유 최적화

#### Google Analytics / Tag Manager

- Google Tag Manager 통합
- Google Analytics 4 통합
- `src/app/layout.tsx`에 스크립트 추가

### 7. 파일 처리

#### 지원 파일 형식

- **이미지**: PNG, JPG, JPEG, GIF, WebP, SVG
- **문서**: PDF, DOCX (Word), PPTX (PowerPoint), XLSX (Excel)
- **텍스트**: TXT, MD, 코드 파일
- **기타**: HWP (한글 문서)

#### 파일 미리보기 컴포넌트

- `src/components/file-preview/`: 각 파일 형식별 뷰어 컴포넌트

---

## 아키텍처 패턴

### 1. App Router 구조

#### 서버 컴포넌트 vs 클라이언트 컴포넌트

- **서버 컴포넌트** (기본): 데이터 페칭, SEO 최적화
- **클라이언트 컴포넌트** (`'use client'`): 인터랙티브 기능, 상태 관리

#### 라우팅

- 파일 시스템 기반 라우팅
- 동적 라우트: `[slug]`, `[category]`
- 라우트 그룹: `(main)` (URL에 영향 없음)

### 2. API Routes 패턴

#### 구조

```typescript
// src/app/api/example/route.ts
import { NextRequest, NextResponse } from "next/server";

export async function POST(request: NextRequest) {
  try {
    // 요청 본문 파싱
    const body = await request.json();

    // 환경 변수 검증
    const apiKey = process.env.API_KEY;
    if (!apiKey) {
      return NextResponse.json(
        { error: "API 키가 설정되지 않았습니다." },
        { status: 500 }
      );
    }

    // 비즈니스 로직 처리
    // ...

    // 응답 반환
    return NextResponse.json({ success: true, data: result });
  } catch (error) {
    console.error("API 오류:", error);
    return NextResponse.json(
      { error: "서버 오류가 발생했습니다." },
      { status: 500 }
    );
  }
}
```

#### 에러 처리 패턴

- Try-catch로 에러 캡처
- 콘솔 로깅 (개발 환경)
- 사용자 친화적인 에러 메시지 반환
- 적절한 HTTP 상태 코드 사용

### 3. 상태 관리 패턴

#### 클라이언트 상태

- **React Hooks**: `useState`, `useEffect`
- **LocalStorage**: 영구 저장 (타이머, 알람 등)

#### 서버 상태

- **서버 컴포넌트**: 직접 데이터 페칭
- **API Routes**: 클라이언트에서 호출

### 4. 스타일링 패턴

#### Tailwind CSS 사용

- 유틸리티 클래스 우선
- 다크모드: `dark:` 접두사
- 반응형: `sm:`, `md:`, `lg:`, `xl:` 브레이크포인트

#### CSS 변수

- `globals.css`에 정의
- 다크모드 변수 오버라이드

---

## 개발 가이드

### 1. 프로젝트 설정

#### 초기 설정

```bash
# 의존성 설치
pnpm install

# 환경 변수 설정
cp .env.example .env.local
# .env.local 파일 편집하여 실제 값 입력

# 개발 서버 실행
pnpm run dev
```

#### 개발 서버 접속

- URL: `http://localhost:3000`
- Turbopack 사용으로 빠른 핫 리로드

### 2. 새 페이지 추가

#### 페이지 생성

```typescript
// src/app/new-page/page.tsx
export default function NewPage() {
  return (
    <div>
      <h1>새 페이지</h1>
    </div>
  );
}
```

#### 레이아웃 추가 (선택사항)

```typescript
// src/app/new-page/layout.tsx
export default function NewPageLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <h2>페이지 레이아웃</h2>
      {children}
    </div>
  );
}
```

### 3. 새 API 엔드포인트 추가

#### API Route 생성

```typescript
// src/app/api/new-endpoint/route.ts
import { NextRequest, NextResponse } from "next/server";

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    // 처리 로직
    return NextResponse.json({ success: true });
  } catch (error) {
    return NextResponse.json({ error: "오류 발생" }, { status: 500 });
  }
}
```

### 4. 컴포넌트 개발

#### 서버 컴포넌트 (기본)

```typescript
// 서버 컴포넌트는 'use client' 없이 작성
export default function ServerComponent() {
  // 직접 데이터 페칭 가능
  const data = await fetchData();
  return <div>{data}</div>;
}
```

#### 클라이언트 컴포넌트

```typescript
"use client";

import { useState } from "react";

export default function ClientComponent() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>클릭: {count}</button>;
}
```

### 5. 스타일링 가이드

#### Tailwind CSS 사용

```typescript
<div
  className="
  bg-white dark:bg-gray-800
  text-gray-900 dark:text-gray-100
  p-4 rounded-lg shadow-sm
  hover:shadow-md transition-all duration-300
"
>
  내용
</div>
```

#### 반응형 디자인

```typescript
<div
  className="
  grid grid-cols-1
  sm:grid-cols-2
  lg:grid-cols-3
  gap-4
"
>
  {/* 그리드 아이템 */}
</div>
```

### 6. 환경 변수 사용

#### 서버 사이드

```typescript
const apiKey = process.env.GEMINI_API_KEY;
```

#### 클라이언트 사이드

```typescript
const siteUrl = process.env.NEXT_PUBLIC_SITE_URL;
```

---

## 배포 및 운영

### 1. 빌드

#### 프로덕션 빌드

```bash
pnpm run build
```

#### 빌드 산출물

- `.next/` 폴더에 생성
- 정적 파일 최적화
- 코드 스플리팅 자동 적용

### 2. 배포 플랫폼

#### Vercel (권장)

- Next.js 최적화
- 자동 배포 (Git 연동)
- 환경 변수 설정 지원

#### 기타 옵션

- Netlify
- AWS Amplify
- 자체 서버 (Node.js 필요)

### 3. 환경 변수 설정 (프로덕션)

#### Vercel 설정

1. 프로젝트 설정 → Environment Variables
2. 각 환경 변수 추가
3. 환경별 설정 가능 (Production, Preview, Development)

### 4. 캐시 전략

#### 정적 파일

- `/_next/static/`: 1년 캐시 (immutable)
- 이미지/아이콘: 1일 캐시

#### 페이지

- 짧은 캐시 (10초)
- stale-while-revalidate (59초)

### 5. 모니터링

#### Google Analytics

- 페이지뷰 추적
- 이벤트 추적
- 사용자 행동 분석

#### 에러 로깅

- 콘솔 로깅 (개발 환경)
- 프로덕션: 외부 서비스 연동 권장 (Sentry 등)

---

## 문제 해결 가이드

### 1. 환경 변수 관련

#### 문제: 환경 변수가 읽히지 않음

**해결 방법:**

1. `.env.local` 파일이 프로젝트 루트에 있는지 확인
2. 서버 재시작 (환경 변수 변경 시 필수)
3. 변수명 확인 (`NEXT_PUBLIC_` 접두사 필요 여부 확인)

#### 문제: 클라이언트에서 환경 변수 접근 불가

**해결 방법:**

- `NEXT_PUBLIC_` 접두사 추가
- 또는 API Route를 통해 간접 접근

### 2. Notion API 관련

#### 문제: Notion API 호출 실패

**해결 방법:**

1. API 키 확인 (`NOTION_API_KEY`)
2. 데이터베이스 ID 확인 (`NOTION_DATABASE_ID`)
3. 데이터베이스에 통합 연결 확인
4. 데이터베이스 스키마 확인 (필수 속성 존재 여부)

### 3. 이미지 최적화 관련

#### 문제: 외부 이미지가 표시되지 않음

**해결 방법:**

1. `next.config.ts`의 `remotePatterns` 확인
2. 이미지 URL 형식 확인
3. CORS 문제 확인

### 4. PWA 관련

#### 문제: Service Worker가 작동하지 않음

**해결 방법:**

1. HTTPS 사용 확인 (로컬 개발 제외)
2. 브라우저 개발자 도구 → Application → Service Workers 확인
3. 캐시 삭제 후 재등록

### 5. 빌드 오류

#### 문제: TypeScript 타입 오류

**해결 방법:**

1. 타입 정의 확인
2. `any` 타입 사용 최소화
3. 타입 가드 사용

#### 문제: 모듈을 찾을 수 없음

**해결 방법:**

1. `pnpm install` 재실행
2. `node_modules` 삭제 후 재설치
3. 패키지 버전 확인

---

## 참고 자료

### 공식 문서

- [Next.js 공식 문서](https://nextjs.org/docs)
- [React 공식 문서](https://react.dev)
- [Tailwind CSS 공식 문서](https://tailwindcss.com/docs)
- [TypeScript 공식 문서](https://www.typescriptlang.org/docs)

### 프로젝트 내 문서

- [기술 스택 가이드](./기술-스택-가이드.md)
- [네이버 검색광고 API 설정 가이드](./naver-search-ad-api-setup.md)
- [카카오 애드핏 통합 가이드](./카카오-애드핏-통합-가이드.md)
- [디자인 적용 계획](./디자인-적용-계획.md)

### 외부 API 문서

- [Google Gemini API](https://ai.google.dev/docs)
- [Notion API](https://developers.notion.com)
- [네이버 검색광고 API](https://naver.github.io/searchad-apidoc/)

---

## 업데이트 이력

- **2026년 1월**: 초기 문서 작성
  - 프로젝트 기술 스택 정리
  - 아키텍처 패턴 문서화
  - 개발 가이드 작성

---

**이 문서는 프로젝트의 기술적인 사항을 정리한 참고 자료입니다.  
비슷한 프로젝트를 진행할 때 이 문서를 참고하여 개발을 쉽게 진행할 수 있습니다.**
