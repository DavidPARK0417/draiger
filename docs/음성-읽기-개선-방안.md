# 음성 읽기 기능 개선 방안 (edge-TTS 참고)

> 작성일: 2026년 1월  
> 참고: [edge-TTS GitHub](https://github.com/kss2002/edge-TTS)

## 📋 개요

현재 Web Speech API를 사용하는 음성 읽기 기능을 Microsoft Edge TTS 엔진을 활용한 edge-TTS로 개선하는 방안을 제시합니다.

---

## 🎯 개선 목표

1. **고품질 음성**: 더 자연스럽고 인간적인 목소리 제공
2. **다양한 보이스**: 선희, 인준, 현수 등 여러 한국어 보이스 선택 가능
3. **향상된 품질**: 단어 단위 끊김 문제 해결
4. **무제한 사용**: 글자 수 제한 없이 사용 가능

---

## 🔧 구현 방안

### 방안 1: Next.js API Route + edge-tts 서버 (권장)

**장점:**
- 완전한 제어 가능
- 고품질 음성 보장
- 다양한 보이스 선택 가능

**단점:**
- 별도의 Python 서버 필요
- 서버 리소스 사용

**구현 방법:**

#### 1-1. Python 서버 구축 (별도 서버)

```python
# server/edge_tts_server.py
from fastapi import FastAPI, HTTPException
from fastapi.responses import StreamingResponse
import edge_tts
import asyncio
import io

app = FastAPI()

@app.post("/api/tts")
async def text_to_speech(text: str, voice: str = "ko-KR-SunHiNeural", rate: str = "+0%"):
    try:
        communicate = edge_tts.Communicate(text, voice, rate=rate)
        audio_data = b""
        async for chunk in communicate.stream():
            if chunk["type"] == "audio":
                audio_data += chunk["data"]
        
        return StreamingResponse(
            io.BytesIO(audio_data),
            media_type="audio/mpeg",
            headers={"Content-Disposition": "attachment; filename=speech.mp3"}
        )
    except Exception as e:
        raise HTTPException(status_code=500, detail=str(e))
```

#### 1-2. Next.js API Route에서 호출

```typescript
// src/app/api/tts/route.ts
export async function POST(request: Request) {
  const { text, voice = 'ko-KR-SunHiNeural', rate = '+0%' } = await request.json();
  
  try {
    const response = await fetch('http://localhost:8000/api/tts', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text, voice, rate }),
    });
    
    return new Response(response.body, {
      headers: {
        'Content-Type': 'audio/mpeg',
      },
    });
  } catch (error) {
    return new Response('TTS 오류 발생', { status: 500 });
  }
}
```

---

### 방안 2: 브라우저에서 직접 edge-tts API 호출 (간단)

**장점:**
- 별도 서버 불필요
- 구현 간단

**단점:**
- Microsoft Edge API 의존
- CORS 이슈 가능성

**구현 방법:**

```typescript
// src/components/TextToSpeechEdge.tsx
'use client';

import { useState } from 'react';

export default function TextToSpeechEdge({ text }: { text: string }) {
  const [isPlaying, setIsPlaying] = useState(false);
  const audioRef = useRef<HTMLAudioElement | null>(null);

  const handlePlay = async () => {
    try {
      // edge-tts API 직접 호출 (CORS 이슈 가능)
      const response = await fetch(
        `https://speech.platform.bing.com/consumer/speech/synthesize/readaloud/voices/list?trustedclienttoken=6A5AA1D4EAFF4E9FB37E23D68491D6F4`,
        {
          method: 'GET',
          headers: {
            'User-Agent': 'Mozilla/5.0',
          },
        }
      );
      
      // 실제 구현은 더 복잡함
      // 대신 방안 1 권장
    } catch (error) {
      console.error('TTS 오류:', error);
    }
  };

  return (
    <button onClick={handlePlay}>
      {isPlaying ? '정지' : '재생'}
    </button>
  );
}
```

---

### 방안 3: Web Speech API 개선 (현재 방식 유지 + 개선)

**장점:**
- 별도 서버 불필요
- 즉시 구현 가능
- 브라우저 네이티브 지원

**단점:**
- 품질 제한
- 보이스 선택 제한

**개선 사항:**

1. **더 나은 보이스 선택**
```typescript
// 한국어 보이스 목록 가져오기
const voices = window.speechSynthesis.getVoices();
const koreanVoices = voices.filter(voice => 
  voice.lang.startsWith('ko') || voice.lang.includes('Korean')
);

// 가장 자연스러운 보이스 선택
const bestVoice = koreanVoices.find(voice => 
  voice.name.includes('Neural') || voice.name.includes('Premium')
) || koreanVoices[0];
```

2. **SSML 사용 (지원되는 경우)**
```typescript
// SSML로 더 자연스러운 읽기
const ssmlText = `
  <speak>
    <prosody rate="${speechRate}">
      ${text}
    </prosody>
  </speak>
`;
```

---

## 🎨 권장 구현: 하이브리드 방식

**현재 Web Speech API 유지 + edge-TTS 옵션 제공**

### 구현 계획

1. **기본**: Web Speech API 사용 (현재 방식)
2. **고급**: edge-TTS 서버 연동 옵션 제공
3. **사용자 선택**: 설정에서 TTS 엔진 선택 가능

```typescript
// src/components/TextToSpeech.tsx
interface TextToSpeechProps {
  content: string;
  title: string;
  metaDescription?: string;
  engine?: 'web-speech' | 'edge-tts'; // 엔진 선택
  voice?: string; // edge-tts 보이스 선택
}

export default function TextToSpeech({ 
  content, 
  title, 
  metaDescription,
  engine = 'web-speech',
  voice = 'ko-KR-SunHiNeural'
}: TextToSpeechProps) {
  // engine에 따라 다른 구현 사용
  if (engine === 'edge-tts') {
    return <TextToSpeechEdge content={content} voice={voice} />;
  }
  
  // 기본 Web Speech API 사용
  return <TextToSpeechWeb content={content} />;
}
```

---

## 📦 필요한 패키지

### Python 서버 (방안 1)

```bash
pip install edge-tts fastapi uvicorn
```

### Next.js (방안 1)

```bash
# 추가 패키지 불필요 (기본 fetch 사용)
```

---

## 🚀 단계별 구현 가이드

### Phase 1: Web Speech API 개선 (즉시 가능)

1. ✅ 더 나은 보이스 선택 로직 추가
2. ✅ SSML 지원 확인 및 적용
3. ✅ 현재 개선 사항 유지 (문장 단위 읽기, 일시정지 등)

### Phase 2: edge-TTS 서버 구축 (선택 사항)

1. Python 서버 구축
2. Next.js API Route 추가
3. 프론트엔드 연동

### Phase 3: 사용자 선택 기능 추가

1. 설정 UI에 TTS 엔진 선택 옵션 추가
2. 보이스 선택 옵션 추가
3. 사용자 설정 저장

---

## 💡 참고 자료

- [edge-TTS GitHub](https://github.com/kss2002/edge-TTS)
- [edge-tts 공식 문서](https://github.com/rany2/edge-tts)
- [Web Speech API 문서](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)

---

## ⚠️ 주의사항

1. **CORS 이슈**: edge-TTS API를 직접 호출할 경우 CORS 문제 발생 가능
2. **서버 리소스**: Python 서버를 구축할 경우 서버 리소스 고려 필요
3. **비용**: 별도 서버 운영 시 비용 발생 가능
4. **법적 고려**: Microsoft Edge TTS 사용 시 라이센스 확인 필요

---

## 🎯 결론 및 권장사항

**즉시 개선 가능한 사항:**
- ✅ Web Speech API의 더 나은 보이스 선택
- ✅ 현재 구현된 문장 단위 읽기, 일시정지 기능 유지

**장기 개선 사항:**
- edge-TTS 서버 구축 (고품질 음성이 필요한 경우)
- 사용자 선택 기능 추가

**권장 접근:**
현재 Web Speech API 개선을 먼저 완료하고, 필요시 edge-TTS 서버를 추가하는 단계적 접근을 권장합니다.

---

## 🔧 근본적인 개선 방안 (2026년 1월)

### 문제점 분석

#### 1. 속도 문제
- **현재 상황**: 기본 속도 2.0배속이 설정되어 있으나, 1.0배속이나 1.5배속으로 변경하면 기계가 읽는 것처럼 딱딱하고 자연스럽지 않음
- **원인 분석**:
  - Web Speech API의 `rate` 속성은 브라우저마다 구현이 다름
  - 낮은 속도(0.5~1.5)에서는 음성 합성 엔진이 단어를 끊어서 읽거나 부자연스러운 리듬 생성
  - 기본 보이스의 품질이 낮을 경우 더욱 두드러짐

#### 2. 속도 변경 시 위치 문제
- **현재 상황**: 읽기 도중 속도를 변경하면 이전 문장으로 돌아가서 다시 읽는 문제 발생
- **원인 분석**:
  - 현재 코드는 `currentPlayingIndexRef`를 사용하여 문장 단위로만 추적
  - 정확한 재생 위치(문장 내 단어 위치)를 추적하지 못함
  - 속도 변경 시 `cancel()` 후 현재 문장부터 다시 읽기 시작

---

### 해결 방안

#### 방안 1: 속도 범위 재조정 및 기본값 변경 (즉시 적용 가능)

**목표**: 기본 속도 1.0을 기준으로 느리게(0.5~1.0) 또는 빠르게(1.0~3.0) 설정 가능하게 함

**구현 방법**:

1. **기본 속도 변경**: 2.0 → 1.0
2. **속도 범위 확장**: 0.5 ~ 3.0 (기존 0.5 ~ 2.0)
3. **보이스 품질 개선**: Neural 보이스 우선 선택 (이미 구현됨)
4. **Pitch 조절**: 낮은 속도에서도 자연스러운 느낌을 위해 pitch를 약간 조절

```typescript
// 기본 속도 변경
const [speechRate, setSpeechRate] = useState(1.0); // 2.0 → 1.0

// 속도에 따른 pitch 조절
const getPitchForRate = (rate: number): number => {
  // 낮은 속도(0.5~1.0)에서는 pitch를 약간 높여서 더 자연스럽게
  if (rate < 1.0) {
    return 1.1; // 약간 높은 pitch
  } else if (rate > 2.0) {
    return 0.9; // 빠른 속도에서는 pitch를 낮춤
  }
  return 1.0; // 기본값
};

// utterance 생성 시
utterance.pitch = getPitchForRate(speechRate);
```

**장점**:
- 즉시 적용 가능
- 코드 변경 최소화
- 사용자가 원하는 속도 범위 제공

**단점**:
- Web Speech API의 한계로 완벽한 해결은 어려울 수 있음

---

#### 방안 2: 정확한 재생 위치 추적 (근본적 해결)

**목표**: 속도 변경 시 정확한 위치(문장 내 단어 위치)부터 이어서 읽기

**구현 방법**:

1. **`onboundary` 이벤트 활용**: SpeechSynthesisUtterance의 `onboundary` 이벤트로 단어/문장 경계 추적
2. **정확한 위치 저장**: 현재 재생 중인 문장과 문장 내 단어 인덱스 저장
3. **속도 변경 시 정확한 위치부터 재생**: 저장된 위치부터 나머지 텍스트 재생

```typescript
// 위치 추적을 위한 ref 추가
const currentCharIndexRef = useRef(0); // 현재 재생 중인 문자 위치
const currentWordInSentenceRef = useRef(0); // 현재 문장 내 단어 인덱스
const sentenceStartCharIndexRef = useRef(0); // 현재 문장의 시작 문자 위치

// utterance 생성 시 onboundary 이벤트 추가
utterance.onboundary = (event: SpeechSynthesisEvent) => {
  if (event.name === 'word') {
    // 단어 경계에서 현재 위치 업데이트
    currentCharIndexRef.current = event.charIndex;
    currentWordInSentenceRef.current = event.charIndex - sentenceStartCharIndexRef.current;
    
    if (process.env.NODE_ENV === 'development') {
      console.log('단어 경계:', {
        charIndex: event.charIndex,
        wordIndex: currentWordInSentenceRef.current,
        text: event.utterance.text.substring(0, event.charIndex)
      });
    }
  }
};

// 속도 변경 시 정확한 위치부터 재생
const handleRateChange = (newRate: number) => {
  if (isPlaying && textPartsRef.current.length > 0) {
    isRateChangingRef.current = true;
    
    // 현재 재생 중인 utterance 취소
    synthRef.current?.cancel();
    
    // 현재 재생 중인 문장과 문장 내 위치 가져오기
    const currentSentenceIndex = currentPlayingIndexRef.current;
    const currentSentence = textPartsRef.current[currentSentenceIndex];
    const currentCharIndex = currentCharIndexRef.current;
    
    // 현재 위치부터 문장 끝까지의 텍스트 추출
    const textFromCurrentPosition = currentSentence.substring(currentCharIndex);
    
    // 나머지 문장들도 포함하여 재생할 텍스트 생성
    const remainingText = [
      textFromCurrentPosition,
      ...textPartsRef.current.slice(currentSentenceIndex + 1)
    ].join(' ');
    
    // 새로운 속도로 재생
    const newUtterance = new SpeechSynthesisUtterance(remainingText);
    newUtterance.rate = newRate;
    newUtterance.pitch = getPitchForRate(newRate);
    
    // 재생 시작
    synthRef.current?.speak(newUtterance);
  }
};
```

**장점**:
- 정확한 위치 추적 가능
- 속도 변경 시 끊김 없는 재생
- 사용자 경험 크게 개선

**단점**:
- 브라우저별 `onboundary` 이벤트 지원 차이 (Chrome/Edge는 지원, Safari는 제한적)
- 구현 복잡도 증가

**브라우저 호환성**:
- ✅ Chrome/Edge: 완전 지원
- ⚠️ Safari: 부분 지원 (문장 경계만)
- ❌ Firefox: 미지원

---

#### 방안 3: 하이브리드 접근 (권장)

**목표**: 브라우저 호환성을 고려한 최적의 해결책

**구현 방법**:

1. **브라우저 감지**: `onboundary` 이벤트 지원 여부 확인
2. **지원 브라우저**: `onboundary` 이벤트로 정확한 위치 추적
3. **미지원 브라우저**: 현재 문장부터 재생 (기존 방식 유지하되 개선)

```typescript
// 브라우저 호환성 확인
const supportsBoundaryEvents = (): boolean => {
  try {
    const utterance = new SpeechSynthesisUtterance('test');
    return 'onboundary' in utterance;
  } catch {
    return false;
  }
};

const isBoundarySupported = useRef(supportsBoundaryEvents());

// 속도 변경 핸들러
const handleRateChange = (newRate: number) => {
  if (isPlaying && textPartsRef.current.length > 0) {
    isRateChangingRef.current = true;
    synthRef.current?.cancel();
    
    if (isBoundarySupported.current) {
      // 정확한 위치 추적 방식 사용
      handleRateChangeWithBoundary(newRate);
    } else {
      // 현재 문장부터 재생 (개선된 방식)
      handleRateChangeWithoutBoundary(newRate);
    }
  }
};
```

**장점**:
- 모든 브라우저에서 작동
- 지원 브라우저에서는 최적의 경험 제공
- 점진적 개선 가능

---

### 구현 우선순위

#### Phase 1: 즉시 적용 (1일)
1. ✅ 기본 속도 1.0으로 변경
2. ✅ 속도 범위 0.5 ~ 3.0으로 확장
3. ✅ Pitch 조절 함수 추가
4. ✅ 보이스 선택 로직 개선 (이미 구현됨)

#### Phase 2: 위치 추적 개선 (2-3일)
1. ✅ `onboundary` 이벤트 지원 확인
2. ✅ 정확한 위치 추적 구현
3. ✅ 속도 변경 시 정확한 위치부터 재생
4. ✅ 브라우저 호환성 테스트

#### Phase 3: 추가 개선 (선택 사항)
1. 사용자 설정 저장 (로컬 스토리지)
2. 속도 프리셋 제공 (느림/보통/빠름)
3. 음성 품질 향상 (edge-TTS 서버 구축)

---

### 예상 효과

#### 속도 문제 해결
- ✅ 기본 속도 1.0으로 변경하여 자연스러운 읽기 제공
- ✅ 0.5 ~ 3.0 범위로 확장하여 다양한 속도 선택 가능
- ✅ Pitch 조절로 낮은 속도에서도 자연스러운 느낌

#### 위치 문제 해결
- ✅ `onboundary` 이벤트로 정확한 위치 추적
- ✅ 속도 변경 시 끊김 없는 재생
- ✅ 사용자 경험 크게 개선

---

### 테스트 계획

1. **브라우저별 테스트**:
   - Chrome/Edge: `onboundary` 이벤트 정상 작동 확인
   - Safari: 부분 지원 확인
   - Firefox: 폴백 방식 작동 확인

2. **속도 테스트**:
   - 0.5x ~ 3.0x 범위에서 자연스러운 읽기 확인
   - 속도 변경 시 끊김 없는 재생 확인

3. **사용자 테스트**:
   - 실제 사용자 피드백 수집
   - 속도 및 위치 문제 해결 여부 확인

---

### 참고 자료

- [Web Speech API - SpeechSynthesisUtterance](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisUtterance)
- [Web Speech API - SpeechSynthesisEvent](https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisEvent)
- [onboundary 이벤트 브라우저 호환성](https://caniuse.com/speech-synthesis)

